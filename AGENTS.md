# zeta-stakeholder AGENTS

1. Preserve imported Rust history and explicit provenance docs; do not present this repo as greenfield work.
2. This repo is spike-only until the toolchain is proven.
3. Planned commands after promotion:
   - `define commands only after toolchain viability is proven`
4. Keep `origin` intended for `stakeholder-circus/zeta-stakeholder` and `upstream` pointed at `https://github.com/giacomo-b/rust-stakeholder`.
5. Do not present this repo as a parity target until toolchain viability, packaging discipline, and deterministic fixture compatibility are proven.
6. Do not hide missing behavior behind placeholders; record it in `GAPS.md` instead.
