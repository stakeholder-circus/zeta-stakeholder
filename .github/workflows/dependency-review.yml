name: dependency-review

on:
  pull_request:

permissions:
  contents: read
  pull-requests: read

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dependency review is advisory for this newly published repo until GitHub dependency graph is enabled in repository security settings."
